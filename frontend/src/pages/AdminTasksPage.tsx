/*
This file renders the public admin page for creating and editing prepared tasks.
Edit this file when admin task page framing or top-level messaging changes.
Copy this file as a starting point when adding another simple public admin-style page.
*/

import { AdminTasksWorkspace } from "../features/tasks/AdminTasksWorkspace";

export function AdminTasksPage() {
  return <AdminTasksWorkspace />;
}
