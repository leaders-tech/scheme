# This file shows the backend env values for local development.
# Edit this file when the backend needs another env variable.
# Copy one line here when you add another backend env setting.
APP_MODE=dev
APP_HOST=localhost
APP_PORT=8000
DB_PATH=./dev.sqlite3
COOKIE_SECRET=change-this-secret
FRONTEND_ORIGIN=http://localhost:5173
